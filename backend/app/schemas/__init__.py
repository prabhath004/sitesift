"""Pydantic schemas (API request/response models).

Only shared identifiers and status enums exist in the foundation, in
``common.py``. Entity schemas are added by the agent that owns the entity.
"""
