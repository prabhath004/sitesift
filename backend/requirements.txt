# SiteSift backend — foundation dependencies.
# Feature agents may add dependencies here (langgraph, pypdf, ...) as needed.
# Keep this list minimal: the foundation must install and run without AI keys.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0
SQLAlchemy>=2.0,<3.0
alembic>=1.13,<2.0
langgraph>=0.2,<1.0
PyMuPDF>=1.24,<2.0
python-multipart>=0.0.9,<1.0

# Postgres driver. SQLite is the default in local development, so this is only
# exercised when DATABASE_URL points at Postgres (docker compose does).
psycopg[binary]>=3.2,<4.0

# Dev / quality tooling
pytest>=8.3,<9.0
httpx>=0.27,<1.0
ruff>=0.7,<1.0
mypy>=1.11,<2.0

# CSV upload (multipart/form-data) for candidate-site import.
